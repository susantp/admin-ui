import { Metadata } from "next"
import Users from "@/auth/presentation/pages/users"

export const metadata: Metadata = {
  title: "User Management",
  description: "User Management screen",
}

export default Users
